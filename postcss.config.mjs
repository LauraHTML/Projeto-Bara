const config = {
  theme: {
    extend: {
      fontFamily: {
        titulo: ['var(--font-ruina)'], 
        paragrafo: ['var(--font-helvetica)'],
      },
    },
  },
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
